rootProject.name = 'SakhiC'
// React Native 0.72+ autolinking is handled by the Gradle Plugin
// Try to apply native_modules.gradle if it exists, otherwise autolinking will handle it
try {
    def nativeModulesFile = file("../node_modules/@react-native-community/cli-platform-android/native_modules.gradle")
    if (nativeModulesFile.exists()) {
        apply from: nativeModulesFile
        applyNativeModulesSettingsGradle(settings)
    }
} catch (Exception e) {
    // Autolinking will handle native modules
}
include ':app'
// Manually include react-native-view-shot if autolinking doesn't work
include ':react-native-view-shot'
project(':react-native-view-shot').projectDir = new File(rootProject.projectDir, '../node_modules/react-native-view-shot/android')
includeBuild('../node_modules/@react-native/gradle-plugin')
